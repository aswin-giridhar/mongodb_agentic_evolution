# Switch the dashboard from the scripted mock to the live backend.
# Default (unset / "mock") uses the scripted timeline in src/lib/mockEventSource.ts.
NEXT_PUBLIC_EVENT_SOURCE=mock

# Where the backend lives. Used only when NEXT_PUBLIC_EVENT_SOURCE=sse.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
